---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-config-pv
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi
  storageClassName: local-storage
  hostPath:
    path: /var/lib/mysql-config

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-config-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage
  volumeName: mysql-config-pv